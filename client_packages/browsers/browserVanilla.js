let tabBrowser = null;

//TABULADOR INICIO
/*  mp.keys.bind(9, false, function() {
    let istyping = mp.players.local.isTypingInTextChat;
    let islogged = mp.players.local.getVariable('loggedIn');
    if(islogged && !istyping && !mp.game.ui.isPauseMenuActive() && !mp.players.local.phoneStatus) {
        if(tabBrowser == null)
        tabBrowser = mp.browsers.new('package://gui/inicio/inicio.html');
    }
})

mp.keys.bind(27, true, function () {//AL ESCAPE CIERRA LA VENTANA 
    if (tabBrowser) {
        mp.gui.cursor.show(false, false);
        tabBrowser.destroy();
        tabBrowser = null; 
        mp.players.local.clearTasksImmediately();
        mp.players.local.freezePosition(false);   
    }

  });  */